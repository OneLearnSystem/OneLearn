import {esc,link,central,login,signup,signOut,identity,run,resetPassword,recovery,finishRecovery} from './shared.mjs';
const app=document.querySelector('#app');let me=null,dashboard=null;
const products=[['OneEducation','Your whole school, connected.','Attendance, behaviour, timetables, exams and student communities.','£142 / month','£1,450 / year','Free for official secondary schools, after approval.','#17695c','#e4f2ed'],['OneHome','Homework with a home.','Set maths, reading and science homework from ready-made topics. Import students or connect your OneEducation classes.','Free for all','','','#7650ad','#eee7fa'],['OneWeb','A website that feels like your school.','Create pages, choose your colours, upload your logo and publish school news, documents and information.','£43 / month','£432 / year','Free for official secondary schools, after approval.','#427b9c','#e3f0f7'],['OneSixth','Their next chapter starts here.','Student imports, subject choices, class allocation and a dedicated sixth-form portal.','Free for all','','','#b56570','#f7e6e9']];
function shell(body){app.innerHTML=`<main class="platform-page"><nav class="sales-nav"><a class="sales-logo" href="#"><img src="platform-logo.svg" alt="">OneLearn</a><div class="platform-actions"><a href="#products">Our products</a><a href="#contact">Contact us</a><button id="account">${identity()?'My account':'Sign in / invitation'}</button></div></nav>${body}<footer class="sales-footer">OneLearn · OneEducation · OneHome · OneWeb · OneSixth<br>Access is arranged by request. Prices are displayed as supplied; billing, tax treatment and free-school eligibility are confirmed before activation. No payment is taken on this website.</footer></main>`;document.querySelector('#account').onclick=()=>identity()?run(loadAccount):account()}
function home(){shell(`<section class="sales-hero"><span class="eyebrow">One family. Every school’s next chapter.</span><h1>More time for<br><span style="color:#17695c">school life.</span></h1><p class="platform-muted">Four connected products. Your school’s identity. One place to get started.</p><div class="platform-actions"><a class="button" href="#products">Find your fit ↗</a><a href="#contact">Let’s talk about your school</a></div></section><section id="products" class="platform-grid">${products.map(([n,h,d,p,y,f,c,t])=>`<article class="platform-card" style="--product:${c};--tint:${t}"><div class="product-visual" aria-hidden="true"><div class="side"></div><div class="tiles"><i></i><i></i><i></i><i></i></div></div><div class="product-label"><img src="${n.toLowerCase()}-mark.svg" alt=""><h2>${n}</h2></div><h3>${h}</h3><p class="platform-muted">${d}</p><p class="price">${p}</p>${y?`<p>or <strong>${y}</strong></p>`:''}<p><small>${f||'Available to every school through a free account request.'}</small></p><div class="platform-actions"><a class="button" style="background:${c}" href="${esc(link(n,'index.html','',true))}" target="_blank" rel="noopener">Try the demo</a><a href="#contact">Request access →</a></div></article>`).join('')}</section><section class="platform-grid" style="margin-top:50px"><article><span class="eyebrow">A personal start</span><h2>Your school. Your workspace.</h2><p>Tell us what you need. Once approved, you’ll receive a private invitation code. Create your password, verify your email, and open your school’s apps.</p><p>School administrators can update their logo, name, contact information and portal settings. Each school has its own isolated records.</p></article><article class="platform-card" id="contact"><h2>Let’s get you started.</h2><form id="request"><label>Your email<input name="email" type="email" required maxlength="254"></label><label>School or organisation<input name="school" required minlength="2" maxlength="160"></label><p>Which products interest you?</p>${products.map(([n])=>`<label><input type="checkbox" name="product" value="${n}"> ${n}</label>`).join('')}<label><input type="checkbox" name="official"> We are an official secondary school</label><label>How can we help?<textarea name="message" required minlength="10" maxlength="3000"></textarea></label><label class="honeypot" aria-hidden="true">Leave empty<input name="website" tabindex="-1" autocomplete="off"></label><p><small>Your email and request will be stored privately so OneLearn can review and respond. Please don’t include student information.</small></p><label><input type="checkbox" required> I agree to be contacted about this request.</label><button>Send contact request</button><p id="message" role="status"></p></form></article></section>`);document.querySelector('#request').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target),b=e.submitter;b.disabled=true;try{if(f.get('website'))throw Error('Unable to submit.');await central('ol_contact',{p_email:f.get('email'),p_school:f.get('school'),p_message:f.get('message'),p_products:f.getAll('product'),p_official:f.has('official')},false);e.target.reset();document.querySelector('#message').textContent='Your request has been received. OneLearn will review it and contact you.'}catch(err){document.querySelector('#message').textContent=err.message}finally{b.disabled=false}}}
function account(){shell(`<section class="platform-grid" style="margin-top:45px"><article><span class="eyebrow">Your OneLearn account</span><h1>Let’s open<br>your school.</h1><p>Use your email and password across all four apps. New here? Your invitation code connects your verified account to your school.</p><p>Invitations expire after seven days and can only be used by the email they were issued to.</p></article><article class="platform-card"><form id="auth"><label>Email<input name="email" type="email" required autocomplete="username"></label><label>Password<input name="password" type="password" required minlength="12" autocomplete="current-password"></label><label>Invitation code (new school access)<input name="code" autocomplete="off" spellcheck="false"></label><div class="platform-actions"><button name="action" value="login">Sign in</button><button name="action" value="create" class="secondary">Create password</button></div><p id="auth-status" role="status"></p></form><button id="forgot" class="secondary">Forgot password</button></article></section>`);document.querySelector('#auth').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);e.submitter.disabled=true;try{if(e.submitter.value==='create'){if(!f.get('code').trim())throw Error('Enter the invitation code supplied by OneLearn.');const signed=await signup(f.get('email'),f.get('password'));if(!signed){document.querySelector('#auth-status').textContent='Check your email to verify your account. Then return here, sign in and enter the same invitation code.';return}}else await login(f.get('email'),f.get('password'));if(f.get('code').trim())await central('ol_redeem',{p_code:f.get('code')});await loadAccount()}catch(err){document.querySelector('#auth-status').textContent=err.message}finally{e.submitter.disabled=false}};document.querySelector('#forgot').onclick=()=>run(async()=>{const email=document.querySelector('[name=email]').value;if(!email)throw Error('Enter your email first.');await resetPassword(email);document.querySelector('#auth-status').textContent='Check your email for a password reset link.'})}
async function loadAccount(){me=await central('ol_me');dashboard=me.owner?await central('ol_dashboard'):null;renderAccount()}
function renderAccount(){shell(`<section style="margin-top:40px"><div class="platform-actions"><h1>Your OneLearn.</h1><button id="signout" class="secondary">Sign out</button></div><p>${esc(identity()?.email)}</p><div class="platform-grid">${me.schools.map(s=>`<article class="platform-card"><h2>${esc(s.name)}</h2><p>${esc(s.role)} · ${esc(s.slug)}</p><div class="platform-actions">${products.map(([n])=>`<a class="button" href="${esc(link(n,'index.html',s.slug,false))}">${n}</a>`).join('')}<a href="${esc(link('OneLearn','setup.html',s.slug,false))}">School setup</a></div></article>`).join('')||'<p>No school access yet. Redeem your invitation below.</p>'}</div><form id="redeem" class="platform-card" style="margin-top:20px"><label>Invitation code<input name="code" required></label><button>Connect another school</button></form>${dashboard?ownerHTML():''}</section>`);document.querySelector('#signout').onclick=()=>{signOut();home()};document.querySelector('#redeem').onsubmit=e=>{e.preventDefault();run(async()=>{await central('ol_redeem',{p_code:new FormData(e.target).get('code')});await loadAccount()})};if(dashboard)bindOwner()}
function ownerHTML(){return `<h2>Owner control room</h2><p>Approve requests here, then send the displayed code to the requester yourself. Creating a school does not send an email or charge money.</p><div class="platform-grid"><form id="provision" class="platform-card"><h3>Create a school</h3><label>School name<input name="name" required minlength="2" maxlength="160"></label><label>School address<input name="slug" required pattern="[a-z0-9][a-z0-9-]{2,47}" placeholder="example-academy"></label><label>Administrator email<input name="email" type="email" required></label><input type="hidden" name="request"><button>Create school & invitation</button><p id="provision-status" role="status"></p></form><form id="invite" class="platform-card"><h3>Issue a fresh invitation</h3><label>School<select name="school">${dashboard.schools.filter(s=>s.active).map(s=>`<option value="${s.id}">${esc(s.name)}</option>`).join('')}</select></label><label>Email<input name="email" type="email" required></label><button>Generate invitation code</button><p id="invite-status" role="status"></p></form></div><h3>Contact requests</h3><div class="platform-grid">${dashboard.requests.map(r=>`<article class="platform-card"><small>${esc(r.status)} · ${new Date(r.created_at).toLocaleDateString('en-GB')}</small><h3>${esc(r.school)}</h3><p>${esc(r.email)}<br>${esc(r.products.join(', '))}<br>${r.official?'Requests official secondary school eligibility review':'Standard access request'}</p><p style="white-space:pre-line">${esc(r.message)}</p><button data-request="${r.id}" class="secondary">Use these details</button></article>`).join('')||'<p>No requests yet.</p>'}</div><h3>Schools</h3><div class="scroll"><table><tr><th>School</th><th>Address</th><th>Access</th></tr>${dashboard.schools.map(s=>`<tr><td>${esc(s.name)}</td><td>${esc(s.slug)}</td><td><button data-toggle="${s.id}" data-active="${s.active}">${s.active?'Suspend':'Reactivate'}</button></td></tr>`).join('')}</table></div>`}
function bindOwner(){document.querySelectorAll('[data-request]').forEach(b=>b.onclick=()=>{const r=dashboard.requests.find(r=>r.id===b.dataset.request),f=document.querySelector('#provision');f.elements.name.value=r.school;f.elements.email.value=r.email;f.elements.request.value=r.id;f.elements.slug.value=r.school.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'').slice(0,48);f.scrollIntoView({behavior:'smooth'})});document.querySelector('#provision').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);e.submitter.disabled=true;try{const r=await central('ol_provision',{p_slug:f.get('slug'),p_name:f.get('name'),p_email:f.get('email'),p_request:f.get('request')||null});document.querySelector('#provision-status').textContent='Created. Send this private code to '+r.email+': '+r.code+' (valid 7 days). Copy it before leaving this page.';dashboard=await central('ol_dashboard')}catch(err){document.querySelector('#provision-status').textContent=err.message}finally{e.submitter.disabled=false}};document.querySelector('#invite').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);e.submitter.disabled=true;try{const c=await central('ol_issue',{p_school:f.get('school'),p_email:f.get('email'),p_role:'admin'});document.querySelector('#invite-status').textContent='Send privately to '+f.get('email')+': '+c+' · valid 7 days. Copy now.'}catch(err){document.querySelector('#invite-status').textContent=err.message}finally{e.submitter.disabled=false}};document.querySelectorAll('[data-toggle]').forEach(b=>b.onclick=()=>run(async()=>{if(!confirm('Change access for this school? Suspension blocks all apps and its published website.'))return;await central('ol_school_active',{p_id:b.dataset.toggle,p_active:b.dataset.active!=='true'});await loadAccount()}))}
if(recovery){shell('<section class="platform-card" style="margin-top:40px"><h1>Choose a new password.</h1><form id="recover"><label>New password<input name="password" type="password" minlength="12" required autocomplete="new-password"></label><button>Save new password</button><p id="recovery-status" role="status"></p></form></section>');document.querySelector('#recover').onsubmit=async e=>{e.preventDefault();e.submitter.disabled=true;try{await finishRecovery(new FormData(e.target).get('password'));account();document.querySelector('#auth-status').textContent='Password updated. Sign in with your new password.'}catch(err){document.querySelector('#recovery-status').textContent=err.message;e.submitter.disabled=false}}}else home();
